# general{
#     disable_loading_bar = true
#     hide_cursor = true
# }
#
# background {
#   monitor =
#     path = $HOME/Pictures/wallpapers/Dynamic-Wallpapers/Dark/Forest-Dark.png  # only png supported for now
#     blur_passes = 0
#     blur_size = 0
#     vibrancy = 0.5
#     vibrancy_darkness = 0.5
# }
#
# label {
#     monitor =
#     text =  
#     font_size = 40
#     color = rgba(137, 180, 250, 1.0)
#     font_family = Liberation Sans
#     position = -50, 400
#     halign = right
#     valign = bottom
#     shadow_passes = 5
#     shadow_size = 10
# }
#
# label {
#     monitor =
#     text = $USER  
#     font_size = 28
#     color = rgba(137, 180, 250, 1.0)
#     font_family = Liberation Sans
#     position = -50, 300
#     halign = right
#     valign = bottom
#     shadow_passes = 5
#     shadow_size = 10
# }
#
# label {
#   monitor =
#     color = rgba(137, 180, 250, 1.0)
#     text = cmd[update:1000] echo "<span>$(date '+%A, %d %B')</span>"
#     font_size = 28
#     font_family = Liberation Sans
#     position = -50, 100
#     halign = right
#     valign = bottom
#     shadow_passes = 5
#     shadow_size = 10
# }
#
# label {
#   monitor =
#     text = cmd[update:1000] echo "<span>$(date '+%H:%M:%S')</span>"
#     color = rgba(137, 180, 250, 1.0)
#     font_size = 28
#     font_family = Liberation Sans
#     position = -50, 0 # h,v
#     halign = right
#     valign = bottom
#     shadow_passes = 4
#     shadow_size = 5
#     shadow_boost = 2
#     shadow_color = rgba(0, 0, 0, 0.8)
# }
#
# input-field {
#     monitor =
#     size = 200, 30
#     outline_thickness = 2
#     dots_size = 0.4     # Scale of input-field height, 0.2 - 0.8
#     dots_spacing = 0.2  # Scale of dots' absolute size, 0.0 - 1.0
#     dots_center = true
#     outer_color = rgba(40,40,40, 0.1)
#     inner_color = rgba(137, 180, 250, 0.1)
#     fade_on_empty = true
#     placeholder_text = Enter Password   # Text rendered in the input box when it's empty.
#     hide_input = false
#     position = 0, 500
#     halign = center
#     valign = bottom
# }

$accent = $mauve
$accentAlpha = $mauveAlpha
$font = Electroharmonix bold
$font_color = rgb(32302f)

# GENERAL
general {
    disable_loading_bar = true
    # hide_cursor = true
}

# BACKGROUND
background {
    monitor =
    path = ~/.config/hypr/background/japanese_aesthetic.png
    blur_passes = 0
    color = $base
}

# TIME
label {
    monitor =
    text = cmd[update:30000] echo "$(date +"%R")"
    color = $font_color
    font_size = 90
    font_family = $font
    position = 200, -150
    halign = left
    valign = top
}

# DATE
label {
    monitor =
    text = cmd[update:43200000] echo "$(date +"%A, %d %B %Y")"
    color = $font_color
    font_size = 25
    font_family = $font
    position = 190, -290
    halign = left
    valign = top
}

# INPUT FIELD
input-field {
    monitor =
    size = 340, 35
    outline_thickness = 
    dots_size = 
    dots_spacing = 
    outer_color = 
    dots_center = true
    inner_color = rgb(d0c8a6)
    color = $font_color
    font_family = $font
    fade_on_empty = false
    placeholder_text = | password |
    hide_input = false
    check_color = 
    fail_color = 
    capslock_color = 
    fail_color = $font_color
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    position = 190, 400
    halign = left
    valign = bottom
}

shape {
    monitor =
    size = 340, 35
    position = 190, 450
    color = rgb(d0c8a6)
    outline_thickness = 0
    rounding = -1
    halign = left
    valign = bottom
}
label {
    monitor =
    text = $USER
    font_family = $font
    font_size = 12
    color = $font_color
    position = 284, 458  
    halign = left
    valign = bottom
}
